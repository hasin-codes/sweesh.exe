<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow JIT compilation for V8 JavaScript engine -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    
    <!-- Allow unsigned executable memory (required for Electron) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    
    <!-- Disable library validation (required for node modules) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    
    <!-- Microphone access (CRITICAL for voice transcription) -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
    
    <!-- Apple Events (for global keyboard shortcuts) -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
    
    <!-- Accessibility (required for node-global-key-listener) -->
    <key>com.apple.security.personal-information.accessibility</key>
    <true/>
</dict>
</plist>

